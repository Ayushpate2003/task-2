apiVersion: v1
kind: Service
metadata:
  name: catalogue-service
spec:
  ports:
    - port: 80
      targetPort: 9191
      protocol: TCP
      name: http
  selector:
    app: catalogue
  type: ClusterIP
